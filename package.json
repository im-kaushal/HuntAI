{
  "name": "huntai",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered job search operating system scaffold and core matching engine.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/**/*.test.js",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.5.4"
  }
}
